# Canonical dependency source is pyproject.toml. Install with: pip install -e ".[dev]"
# (numpy removed — it was unused; anthropic moved to the optional "live" extra.)
pyyaml==6.0.3
matplotlib==3.9.4
pytest==9.1.1
